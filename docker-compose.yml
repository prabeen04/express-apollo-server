version: '3'

services:
  app:
    container_name: backend
    restart: always
    build: .
    ports:
      - '4000:4000'
    links:
      - mongo
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app  
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - mongodb_data:/data/db
    ports:
      - '27017:27017'      
volumes:
  mongodb_data:
    driver: local      