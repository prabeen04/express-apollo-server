type Query {
  user: User
  author: Author
  books: [Book]
  getBookById(id: Int!): Book
}

type Author {
  id: String!
  name: String!
}

type User {
  id: String!
  userName: String!
  email: String!
}

type Book {
  id: Int!
  title: String!
  author: Author!
}