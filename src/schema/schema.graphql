type Query {
  user: User
  author: Author
  books: [Book]
}

type Author {
  id: String!
  name: String!
}

type User {
  id: String!
  userName: String!
  email: String!
}

type Book {
  title: String!
  author: Author!
}